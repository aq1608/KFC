# Images

Drop the neighbourhood hen photo here as **`found-chicken.jpg`**.

It's shown in the landing-page hero (`views/index.ejs`). Any reasonably sized
(~600px wide) JPG/PNG works. Until the file exists, the hero image hides itself
gracefully (via an `onerror` handler), so the page never shows a broken image.
